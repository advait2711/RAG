{
    "name": "rag-visualizer",
    "scripts": {
        "build": "cd frontend && npm install --include=dev && npm run build && cd ../backend && npm install --production",
        "start": "cd backend && node server.js"
    }
}